{
  "engines": {
    "npm": "5.6.0",
    "node": "8.10.0"
  },
  "scripts": {
    "build": "check-node-version --package && tsc && (cd temp && zip ../dist/lambda.zip *) && rm -rf temp"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.23",
    "@types/node": "^8.10.45",
    "check-node-version": "^3.3.0",
    "typescript": "^3.4.1"
  }
}
